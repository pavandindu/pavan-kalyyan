#include <stdio.h>

int main()
{
    int X,P,pd=0,ts=0;
    
    scanf("%d%d",&X,&P);
    
    if(X>=0 && X<=10000 && P>=0 &&P<=100)
    {
    while(X>1)
    {
        pd = (X *P)/100;
        ts= ts+X;
        X = X - pd;
        
       
    }
    }
    else 
    return 0;
    
    printf("%d",ts);

    return 0;
}
